package wgh.dubbo.schedule.service;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;

public class BaseService {
	public final Log log = LogFactory.getLog(this.getClass());
}
